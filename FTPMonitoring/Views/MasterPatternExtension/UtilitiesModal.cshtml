
<div class="modal fade" id="MasterPatternExtensionModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">×</button>
                <h4 class="modal-title" id="modalTitle">Formulir Penambahan Data Master Pattern Extension</h4>
            </div>
            <div class="modal-body">
                <div id="errorMessage"></div>
                <form id="MasterPatternExtensionForm">
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <label for="id">ID</label>
                        <input type="text" class="form-control" id="id" placeholder="ID Pattern Extension" disabled="disabled" />
                    </div>
                    <div class="form-group">
                        <label for="name">Nama Extension</label>
                        <input type="text" class="form-control" id="name" placeholder="Nama Pattern Extension" />
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <a class="btn btn-primary" id="btnAdd" onclick="addMasterPatternExtension();">Tambah</a>
                <a class="btn btn-primary" id="btnUpdate" style="display:none;" onclick="updateMasterPatternExtension();">Perbarui</a>
                <a class="btn btn-danger" id="btnDelete" style="display:none;" onclick="deleteMasterPatternExtension();">Hapus</a>
                <button type="button" class="btn btn-default" data-dismiss="modal">Tutup</button>
            </div>
        </div>
    </div>
</div>  
